import React from "react";

const Wider = props => {
    return (
        <div>
            <h2>{props.title}</h2>
            <span>{props.text}</span>
        </div>
    );
};

export default Wider;
